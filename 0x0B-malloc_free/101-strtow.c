#include "main.h"
#include <stdlib.h>

/**
 * **strtow - splits a string into words
 * @str: The pointer to string
 *
 * Return: - If string equals NULL or "", return NULL.
 *  .      - Else,
 */
char **strtow(char *str)
{
	char **p;

	if (str == NULL || !str)
		return (NULL);

	return (p);
}

