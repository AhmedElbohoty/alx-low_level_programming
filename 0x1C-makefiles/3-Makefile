CC = gcc	# the compiler to be used.

SRC = main.c school.c	# the .c files.

OBJ = ${SRC:.c=.o}	# the .o files.

NAME = school

RM = rm

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} ${NAME}

oclean:
	${RM} ${OBJ}

fclean: clean oclean
