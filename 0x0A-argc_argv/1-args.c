#include "main.h"

/**
 * main - prints the count of args
 * @argc: the count of args
 * @argv: array of args
 *
 * Return: (Always) 0
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	_putchar(argc);
	_putchar('\n');

	return (0);
}
