#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: The first integer
 * @b: The seocnd integer
 * Return: the sum result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}

